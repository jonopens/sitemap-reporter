require_relative '../config/environment'

# get a file (either via terminal path or via URL)

# determine if that file is a sitemap_index or a child sitemap
# if sitemap_index, parse out child resource URLs
  # make an array and populate it
# if gzipped, unzip and pass to child sitemap process
# if not index, test each URL and get data
